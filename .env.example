# =========================================
# ShootingStar Configuration
# =========================================

# Google OAuth (required)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=https://shootingstar.scrimvibes.xyz/api/auth/gmail/callback

# Todoist API Token (required)
# Get from: https://todoist.com/app/settings/integrations/developer
TODOIST_TOKEN=your_todoist_api_token

# =========================================
# IMPORTANT: Do NOT set ANTHROPIC_API_KEY
# =========================================
# This app uses Claude CLI with subscription auth, NOT the API.
# The app will fail fast if ANTHROPIC_API_KEY is detected.
# ANTHROPIC_API_KEY=

# Database path (optional, defaults to ./data/shootingstar.db)
DATABASE_PATH=./data/shootingstar.db

# Session secret for OAuth state (auto-generated if not set)
# SESSION_SECRET=generate_a_random_secret_here

# =========================================
# Development Only
# =========================================
# For local development, use:
# GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/gmail/callback
